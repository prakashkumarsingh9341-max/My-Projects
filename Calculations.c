#include <stdio.h>

int main()
{
    /* code */
    int a, b;
    a= 40;
    b= 20;

    printf("a+b = %d\n", a+b);
    printf("a-b = %d\n", a-b);
    printf("a*b = %d\n", a*b);
    printf("a/b = %d\n", a/b);
    /* printf("\a %d",a);*/ 
    /* for decimal places we use float instead of int and %f instead of %d*/

    return 0;
}
